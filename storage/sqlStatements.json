{
  "getAll": ["select productId, name, model, type, price from product"],
  "get": [
    "select productId, name, model, type, price from product",
    "where productId=?"
  ],
  "insert": [
    "insert into product (productId, name, model, type, price)",
    "values(?,?,?,?,?)"
  ],
  "update": [
    "update product set name=?, model=?, type=?, price=?",
    "where productId=?"
  ],
  "remove": ["delete from product where productId=?"],
  "primaryKey": "productId"
}
